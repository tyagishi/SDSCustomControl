import XCTest
@testable import SDSCustomControl

final class SDSCustomControlTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(SDSCustomControl().text, "Hello, World!")
        XCTFail("no tests")
    }
}
